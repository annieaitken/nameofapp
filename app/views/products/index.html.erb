<p id="notice"><%= notice %></p>

<h1>Listing Products</h1>


<div class="container-fluid">
  <div class="row">
    <% @products.each do |product| %>
    <div class="col-sm-4 col-md-4 images">
      <%= image_tag(product.image_urp, class: "img-responsive") %>
    </div>
    <% end %>
  </div>

  <div class="row">
    <% @products.each do |product| %>
    <div class="col-sm-4 col-md-4 info">
      <h1><%= product.name %></h1>
      <p><%= product.description %></p>
      <p><strong><%= product.price %></strong></p>
    </div>
    <% end %>

  </div>
</div>

<br>

<%= link_to 'New Product', new_product_path, { :class=>"newlink" } %>
